<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hosteloutpass">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LastResultActivity"></activity>
        <activity android:name=".StudentRequestActivity" />
        <activity android:name=".FacultyNewRequestActivity" />
        <activity android:name=".FacultyMainActivity" />
        <activity android:name=".FacultyNewEntriesActivity" />
        <activity
            android:name=".ui.login.Temp2"
            android:label="@string/title_activity_temp2" />
        <activity android:name=".FacultyActivity" />
        <activity android:name=".StudentActivity" />
        <activity
            android:name=".FacultyfirebaseActivity"
            android:label="@string/title_activity_faculty"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>